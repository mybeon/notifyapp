import CreateList from '../components/CreateList';
import React from 'react';

const AddList = ({navigation}) => {
  return <CreateList type="create" navigation={navigation} />;
};

export default AddList;
